# Static single-file app — no build step.
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  publish = "src"

# Legacy URL from pre-index.html deploys.
[[redirects]]
  from = "/kiteracer.html"
  to = "/"
  status = 301
